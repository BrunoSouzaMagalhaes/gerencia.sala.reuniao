import { PainelDirective } from './painel.directive';

describe('PainelDirective', () => {
  it('should create an instance', () => {
    const directive = new PainelDirective();
    expect(directive).toBeTruthy();
  });
});
